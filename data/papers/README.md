Place the corpus PDFs listed in the project README here. Filenames must match `eval/dataset.jsonl` `source` values (for example `raft.pdf`, `spanner.pdf`).

PDFs are gitignored. After adding them, run `dsqa ingest data/papers`.
